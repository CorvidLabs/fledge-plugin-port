[plugin]
name = "fledge-plugin-port"
version = "0.1.0"
description = "Find and kill processes bound to a TCP port. Built in Rust."
author = "0xLeif"
license = "MIT"

[[commands]]
name = "port"
description = "Show, kill, or list processes bound to TCP ports"
binary = "target/release/fledge-port"

[capabilities]
exec = false
store = false
metadata = false
